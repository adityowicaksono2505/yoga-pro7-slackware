# /etc/default/zram - Konfigurasi ZRAM untuk Yoga Pro 7
# ---------------------------------------------------

# Aktifkan ZRAM
ZRAM_ENABLE=1

# Jumlah perangkat ZRAM (Kunci ke 1 agar tidak muncul zram1)
ZRAMNUMBER=1

# Ukuran ZRAM dalam KB (14.000.000 KB = ~13.3 GB)
# Menggunakan angka statis lebih aman daripada variabel $MEMTOTAL
ZRAMSIZE=14000000

# Algoritma Kompresi (Wajib zstd untuk Yoga Pro 7)
ZRAMCOMPRESSION=zstd

# Prioritas Swap (100 agar lebih tinggi dari SSD/NVMe)
ZRAMPRIORITY=100
